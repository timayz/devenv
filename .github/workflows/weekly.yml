name: Weekly

on:
  workflow_dispatch:
  schedule:
    - cron: 0 9 * * 1

jobs:
  renovate:
    uses: timayz/.github/.github/workflows/renovate.yml@v3
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
